class observable {
    TextEditor(insertLine, text){
        insertLine() = lineNumber & text
        lineNumber = 'Inserido Texto'
    } 
    removeLine(lineNumber) {
        return text - lineNumber
    } 
    openTextEditor()
    saveTextEditor()
}
